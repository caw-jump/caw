# Postgres — SAME as god-mode-api (Coolify internal). Get from Coolify → god-mode-api → env.
# DATABASE_URL=postgresql://postgres:PASSWORD@lo80k4ccg04wsw0okw0gcs0o:5432/postgres?sslmode=require
# For local dev (optional): use db:up for a local Postgres, or an SSH tunnel to the above host.

# Server
HOST=0.0.0.0
PORT=4321

# Optional: ion-arc.biz / ion-arc.online (Fastify SSR)
# Domain (canonical + JSON-LD)
# SITE_URL=https://ion-arc.biz
# SITE_DOMAIN=https://ion-arc.biz      # alias for SITE_URL
#
# DB table prefix (preferred)
# SITE_DB_PREFIX=ion_arc_biz           # or ion_arc_online
# SITE_TABLE_PREFIX=ion_arc_biz       # alias for SITE_DB_PREFIX
#
# Legacy (still supported)
# SITE_KEY=ion_arc_biz                 # same values as SITE_DB_PREFIX
